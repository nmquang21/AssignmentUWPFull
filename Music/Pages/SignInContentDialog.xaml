<ContentDialog
    x:Class="Music.Pages.SignInContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Music.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SIGN IN"
    PrimaryButtonText="Sign In"  
    CloseButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    CloseButtonClick="ContentDialog_CloseButtonClick">

    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Width="400" Height="300">
        <TextBox Name="emailTextBox" Header="Email :" Margin="0 20 0 20"/>
        <TextBlock x:Name="validate_email" Margin="0 -20 0 20" Foreground="Red" Visibility="Collapsed"/>
        <PasswordBox Name="passwordTextBox" Header="Password : " IsPasswordRevealButtonEnabled="True"/>
        <TextBlock x:Name="validate_password"  Margin="0 0 0 20" Foreground="Red" Visibility="Collapsed"/>
        <!-- Content body -->
        <TextBlock Name="body" TextWrapping="Wrap" Margin="0 10">
            <TextBlock.Text>
               Login to view your Songs, newest Song,...
            </TextBlock.Text>
        </TextBlock>
        <TextBlock x:Name="login_fail" Foreground="Red" HorizontalAlignment="Center" Margin="0 20 0 0"/>
    </StackPanel>
</ContentDialog>